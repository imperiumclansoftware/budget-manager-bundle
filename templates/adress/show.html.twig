{% extends 'base.html.twig' %}

{% block title %}Adress{% endblock %}

{% block body %}
    <h1>Adress</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adress.id }}</td>
            </tr>
            <tr>
                <th>NumVoie</th>
                <td>{{ adress.numVoie }}</td>
            </tr>
            <tr>
                <th>Voie</th>
                <td>{{ adress.voie }}</td>
            </tr>
            <tr>
                <th>Cp</th>
                <td>{{ adress.cp }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ adress.ville }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('adress_index') }}">back to list</a>

    <a href="{{ path('adress_edit', {'id': adress.id}) }}">edit</a>

    {{ include('adress/_delete_form.html.twig') }}
{% endblock %}
